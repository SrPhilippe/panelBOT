$(document).ready(function() {

	var windowW = $(window).width();
	

	if ($('.action.commands').hasClass('active')) {
		var restW = $('.page.commands').innerWidth();
		alert("Resto largura: "+ restW);
	}


});